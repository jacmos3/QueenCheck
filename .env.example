# Public Base Sepolia RPC endpoint. Production/mainnet networks are intentionally absent.
BASE_SEPOLIA_RPC_URL=

# Store the testnet key in Hardhat's encrypted keystore; never commit it here.
# npx hardhat keystore set QUEENCHECK_DEPLOYER_PRIVATE_KEY
